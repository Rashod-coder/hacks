{
    "name": "{{name}}",
    "dependencies": {
      "react": "^17.0.2",
      "react-dom": "^17.0.2"
    },
    "scripts": {
      "start": "{{runCommand}} start",
      "build": "{{runCommand}} build",
      "test": "{{runCommand}} test",
      "eject": "{{runCommand}} eject"
    }
  }
  